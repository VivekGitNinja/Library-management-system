import com.library.model.Book;

public class Book extends com.library.model.Book {
    public Book(String bookCode, String title, String author, String isbn, String genre) {
        super(bookCode, title, author, isbn, genre);
    }
}
